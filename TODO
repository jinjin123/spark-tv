device_preview.dtl and device.dtl: add gallery view 

app/zkf/index.html: debug----cannot update playlist

device.scap and device.shell: scatch the UI

remove scheduler from channels

channels are not specified on individual device, but a global setting

device.dtl and device_preview.dtl need to be changed to take channel list


